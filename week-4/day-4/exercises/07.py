# 1 Create a function get_age(year, month, day)
# Hard-code the current year and month in your code 
# (there are better ways of doing this, but for now it will be enough.)
def get_age(year,month,day):   
    curr_year,curr_month = 2021 , 11 
    year_difference = curr_year - year
    month_difference = curr_month - month

# 2 After calculating the age of a person, the function should return the age
#  (the age is an integer).
